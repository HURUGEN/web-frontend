// Pins the reviewed shared-library release so future pipeline behavior cannot follow a mutable branch.
@Library('jenkins-json-build@v3.2.0') _

// Loads the repository-reviewed configuration that defines Node, BuildKit, and Helm stage ordering.
jenkinsJsonBuild(configFiles: ['ci/jenkins-project.json'])
